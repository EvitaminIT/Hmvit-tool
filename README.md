#Django project steup guide 
 This document explains how to run the Django server on a local system=>

1. Create virtual environment using these command => python3 -m venv venv

2. Activate virtual environment using these command=> source venv/bin/activate

3. Run Server using these command => python manage.py runserver 10.20.40.205:8000

